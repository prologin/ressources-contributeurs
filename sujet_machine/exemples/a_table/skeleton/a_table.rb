require "scanf.rb"
def a_table0( n, m, salle )
  # Inserez votre code ici 
  
end
(n, m) = STDIN.readline.split(" ").map{ |x| x.to_i(10) }
dc = [*0..n-1].map { |dd|
  
  next STDIN.readline.split(//)
  }
salle = dc
a_table0(n, m, salle)
