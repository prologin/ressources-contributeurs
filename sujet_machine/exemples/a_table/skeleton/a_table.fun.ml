let a_table0 n m salle =
  (*  Inserez votre code ici  *)
  ()

let main =
  Scanf.scanf "%d"
  (fun n -> ( Scanf.scanf "%[\n \010]" (fun _ -> ());
              Scanf.scanf "%d"
              (fun m -> ( Scanf.scanf "%[\n \010]" (fun _ -> ());
                          let bk = Array.init n (fun bl -> let bn = Array.init m (fun bo -> Scanf.scanf "%c"
                          (fun bm -> bm)) in
                          ( Scanf.scanf "%[\n \010]" (fun _ -> ());
                            bn)) in
                          let salle = bk in
                          ( a_table0 n m salle;
                            ())))))

